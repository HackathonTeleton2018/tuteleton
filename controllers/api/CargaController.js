/* jshint node: true */
'use strict';

const SOURCE = 'tuteleton:controllers:api:CargaController';
const debug = require('debug')(SOURCE);

module.exports = {
    carga: (req, res, next) => {
        // File is on req.file
    }
};